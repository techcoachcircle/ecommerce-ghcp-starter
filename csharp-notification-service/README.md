## CSharp Notification Service

## Run Tests
1. Build the solution
2. Run tests

## Run the app (to test manually)
1. Use the opensource solution https://sqlitebrowser.org/dl/ or the VSCode extension `Database Client`

2. Run the SQL at `../db/test-notifications.sql`

3. From VS, run the `Program.cs` to check the (fake) notifications being sent.